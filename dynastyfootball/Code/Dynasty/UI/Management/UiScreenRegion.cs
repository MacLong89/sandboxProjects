namespace Dynasty.UI.Management;

/// <summary>
/// Reserved screen zones. Systems must not violate another region's bounds.
/// </summary>
public enum UiScreenRegion
{
	CenterModal,
	TopBar,
	TopRightNotifications,
	MainContent,
	RightSidebar,
	BottomStatus,
	FullScreen
}
