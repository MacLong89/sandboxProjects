namespace Sandbox;

/// <summary>Locomotion clip selection — one clip per tick from the active AI state + planar speed.</summary>
public enum ThornsAnimalLocomotionAnim
{
	Idle,
	Walk,
	Run,
	Attack,
	Death,
}
