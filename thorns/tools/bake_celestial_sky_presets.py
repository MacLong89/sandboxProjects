"""
Thorns sky no longer uses project .vmat files — ThornsCelestialSystem bakes a runtime texture.
Scenes should reference core materials/skybox/skybox_day_01.vmat on SkyBox2D (editor placeholder only).
"""
from __future__ import annotations

print( "Thorns sky uses runtime texture — no project vmat stubs to generate." )
