# Authored data

Canonical content lives in `Code/Core/Content/ContentCatalog.cs` so the portable core and CoreHarness can run without the s&box asset system.

JSON files here are documentation mirrors / future import targets for ports.
